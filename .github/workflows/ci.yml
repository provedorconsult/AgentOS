name: AgentOS CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate AgentOS
        shell: pwsh
        run: ./scripts/validate.ps1
