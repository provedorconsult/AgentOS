name: Environment Health

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 1"

jobs:
  doctor:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: AgentOS doctor
        shell: pwsh
        run: ./scripts/doctor.ps1
