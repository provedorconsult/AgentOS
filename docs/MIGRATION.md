# Migration

## From pre-2.0.0-rc.1 branches

- replace legacy CI expectations with `npm run doctor` and `npm run validate`;
- treat `codex-layer/` as the canonical source for installed `.codex/`;
- update `.harness/current.txt` and `.harness/project-state.json` to the new state rules;
- remove obsolete GitHub publication instructions from local docs.
