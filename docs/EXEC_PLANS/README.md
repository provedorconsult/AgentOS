# Execution Plans

Use this folder for plans that agents can execute and verify.

- `active/`: plans currently in progress.
- `completed/`: plans already executed.

Start from `templates/EXEC_PLAN.md`.
