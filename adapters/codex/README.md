# Codex Adapter

This adapter preserves native Codex usage while keeping AgentOS core universal.

## Install

```powershell
npm run agentos:install-adapter -- codex
```

The installer copies `templates/AGENTS.md` and `templates/.codex/` into the target repository. Existing destinations are backed up before replacement.
