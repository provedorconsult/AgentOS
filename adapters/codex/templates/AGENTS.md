# AgentOS for Codex

Follow the latest user request first, then `docs/SPEC.md`, `docs/PLAN.md`, `.harness/`, this file, `core/rules/` and existing code patterns.

Use SpecPilot Engine for broad work:

1. Discover.
2. Spec.
3. Plan.
4. Implement with closed context.
5. Verify with command evidence.
6. Finish with `docs/REVIEW.md`.

Codex-specific config is under `.codex/`; universal behavior remains under `core/`.
