# Generic IDE Adapter

Use this adapter when the project is driven by an IDE or agent without native AgentOS integration.

## Install

```powershell
npm run agentos:install-adapter -- generic-ide
```

This adapter does not create `.codex/`.
