# IDE Agent Guide

1. Read `AGENTS.md`.
2. Read `docs/SPEC.md` and `docs/PLAN.md`.
3. Select the current task from `.harness/sprints/`.
4. Respect declared `specRefs`, read-only context and editable files.
5. Run verification commands.
6. Record command, exit code and result in `docs/REVIEW.md`.
