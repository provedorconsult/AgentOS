# AgentOS Generic Agent Instructions

Follow the latest user request first, then `docs/SPEC.md`, `docs/PLAN.md`, `.harness/`, `core/rules/` and existing code patterns.

Use closed context from the current task and record verification evidence in `docs/REVIEW.md`.
