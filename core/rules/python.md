# Python Rules

- Use a virtual environment when the project defines one.
- Run import smoke checks for changed modules.
- Use `pytest` when tests exist.
- Avoid new dependencies without justification.
