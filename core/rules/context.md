# Context Rules

- Do not read the entire repository during a bounded task.
- Use declared `specRefs` and `context.readOnly` ranges first.
- Avoid generated directories listed in `agentos.yaml`.
- Split the task when the required context is too broad.
