# SpecPilot Harness

The active SpecPilot runtime is stored under `.harness/`.

- `.harness/project-state.json`
- `.harness/current.txt`
- `.harness/sprints/`
- `.harness/templates/`

This directory exists only to document the harness boundary inside the explicit `specpilot/` surface.
